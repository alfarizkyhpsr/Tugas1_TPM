import 'package:flutter/material.dart';

// Kelas AppColors menyimpan konstanta warna utama aplikasi
// agar mudah digunakan kembali di berbagai halaman tanpa harus menuliskan kode warna berulang kali.
class AppColors {
  // Warna utama (Biru Laut)
  static const Color primaryColor = Color(0xFF006994); 
  
  // Warna kedua (Putih)
  static const Color secondaryColor = Color(0xFFFFFFFF);
  
  // Warna tambahan jika diperlukan, misalnya untuk latar belakang (background)
  static const Color backgroundColor = Color(0xFFF5F5F5);
  static const Color textColor = Color(0xFF333333);
}
